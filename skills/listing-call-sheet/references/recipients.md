# Saved Recipients (distribution defaults)

These are Graeham's standing crew addresses. Use them when creating the Gmail drafts (see `distribution.md`). Confirm per shoot if Graeham names someone different, but default to these.

| Role | Name | Email(s) |
|---|---|---|
| **Videographer** | Wesley | `wes@zephyrmediaworks.com` |
| **Video editor** | (Graeham's video team) | `graehamwattsvideo@gmail.com`, `graehamwattsvideo2@gmail.com` |
| **Owner / approver** | Graeham | `graehamwatts@gmail.com` |

## Draft routing (one draft per box below)

| Draft → To | Contents | Subject |
|---|---|---|
| `wes@zephyrmediaworks.com` | **Videographer copy** (plan header + packet ②) | `Shoot brief — <address> — <shoot date>` |
| `graehamwattsvideo@gmail.com`, `graehamwattsvideo2@gmail.com` | **Editor copy** (full script + packet ④) **+ master** | `Edit sheet — <address>` |
| `graehamwatts@gmail.com` | **Master** (all packets) | `Call sheet (master) — <address>` |

Notes:
- Videographer and editor are **different people here** — send separate drafts, do not merge.
- The editor has two addresses; put both on the editor draft (To: both).
- Drafts only — Graeham reviews and hits Send (the Gmail connector does not auto-send).
- `create_draft` needs plain addresses (`name@example.com`), which these already are.
