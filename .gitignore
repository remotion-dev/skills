# CREDENTIALS — never commit (mirrors the Online Content repo's rules)
github-token.txt
.github-token
github-pat.txt
switchy-token.txt
*.pat
*-pat.txt
*token*.txt
*.token
.claude-credentials/
**/.claude-credentials/
Git Hub PAT Token for claude/

# Local / virtual envs
.venv/
venv/
